import { EntityMetadataMap, EntityDataModuleConfig } from '@ngrx/data';

const entityMetadata: EntityMetadataMap = {};

const pluralNames = {  };

export const appConfig: EntityDataModuleConfig = {
  entityMetadata,
  pluralNames
};
